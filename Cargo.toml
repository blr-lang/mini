[package]
name = "mini"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
tokio = { version = "1.0", features = ["rt", "fs", "io-util", "macros", "rt-multi-thread"] }

[dev-dependencies]
test-case = "3.3.1"
